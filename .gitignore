.build/
.swiftpm/
.DS_Store
# Package.resolved is COMMITTED (supply-chain lock; review its diff on every change)
*.xcodeproj/
dist/
Tests/fixtures/

# Real-voice debugging fixtures (user's actual speech; never commit/push)
fixtures-real/
